extends layout 

block content 

  h1= title 

    ul
      each category in category_list
        li
          a(href=category.url) #{category.name}
          p #{category.url}
      
      else 
        li There are no categories. You can add them at the top
