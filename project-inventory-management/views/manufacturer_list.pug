extends layout 

block content 

  h1= title 

    ul
      each manufacturer in manufacturer_list
        li
          a(href=manufacturer.url) #{manufacturer.name}: #{manufacturer.country}
          
          
      
      else 
        li There are no categories. You can add them at the top
